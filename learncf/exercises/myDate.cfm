
<cfoutput>
  #now()#
  <br>
  #dateFormat(now(), "mm/dd/yyyy")#
  <br>
  #dateFormat(now(), 'mm-dd-yyyy')#
  <br>
  #dateFormat(now(), "dd/mm/yy")#
  <br>
  #dateFormat(now(), 'mmmm dd, yyyy')#
  <br>
  #dateFormat(now(), 'dd mmm yy')#
  <br>
  #dateFormat(now(), 'mm-dd-yy')#
  <br>
  #dateFormat(now(), 'd mmm yy')#
  <br>
  #dateFormat(now(), 'mmm dd yyyy')#
  <br>
  #dateFormat(now(), 'mmmm dd yy')#
  <br>
  #dateFormat(now(), 'dd mmmm yyyy')#
  <br>
</cfoutput>
  